Streamed activities (posts, boosts, edits, deletions) are now pushed to websocket clients only after the database transaction commits, so streamed statuses no longer differ from what the timeline API returns.
