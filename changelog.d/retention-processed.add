The retention worker also prunes processed remote Delete, Undo and Update activities after `processed_activity_days` (7) and remote tombstones after `tombstone_days` (30). Adds a partial index on `objects` for tombstones.
